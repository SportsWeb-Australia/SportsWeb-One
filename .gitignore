# macOS Finder metadata
.DS_Store

# Dependencies (package-lock.json stays tracked)
node_modules/

# Build output
dist/
supabase/.temp/
.vercel

# Local env overrides (this session's develop-branch testing config, etc.)
.env
.env.local
.env.*.local
