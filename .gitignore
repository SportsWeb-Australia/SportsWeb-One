# macOS Finder metadata
.DS_Store

# Dependencies (package-lock.json stays tracked)
node_modules/

# Build output
dist/
supabase/.temp/

# Local environment (Supabase URL + anon key). Never commit these.
.env
.env.*
!.env.example
